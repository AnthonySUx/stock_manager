// Enhanced Soft Neumorphism design system — muted warm low-brightness variant
// Reduced brightness, saturation, and glare to reduce eye strain
// Inspired by Emil Kowalski's design engineering philosophy

export const colors = {
  // Core — lowered brightness from #FDF1F0 to gentle muted taupe
  bg: '#F2E2DF',        // 低亮暖灰粉背景
  surface: '#F2E2DF',   // 表面与背景同色
  surfaceBorder: 'rgba(255,255,255,0.32)',

  // Text — kept readable but slightly warmed
  textPrimary: '#442F2C',     // 深棕红（略深一点补偿背景变暗）
  textSecondary: '#806B67',  // 中灰棕
  textMuted: '#A6928E',     // 浅灰棕

  // Accent (柔和陶土橙) — lowered saturation from #FF6B57
  accent: '#DE705F',
  accentLight: '#E88A7A',
  accentBg: '#F7E4E0',

  // Semantic — kept vibrant but small area only
  success: '#2DBD6F',
  successLight: '#D4EEDC',
  successBg: '#E6F5EC',

  warning: '#E8993E',
  warningLight: '#F7E8D0',
  warningBg: '#FBF3E6',

  danger: '#E84A58',
  dangerLight: '#F5D8DC',
  dangerBg: '#FBEAEC',

  // Shadow system (warm neumorphism) — slightly deepened
  shadowDark: '#D2BDB9',   // 暗影加深
  shadowDark2: '#CAB3AF',
  shadowLight: '#FCF4F2',  // 非纯白高光
  shadowInset: '#DCC9C5',  // 凹陷阴影

  // Misc
  white: '#FCF4F2',
  chipBg: '#EBD7D4',
  chipBorder: 'rgba(255,255,255,0.28)',
  inputBg: '#E9D7D4',
  inputBorder: '#DCC9C5',
  overlay: 'rgba(0,0,0,0.06)',
  divider: 'rgba(0,0,0,0.07)',
};

export const spacing = {
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 20,
  xxl: 24,
  xxxl: 32,
};

export const radius = {
  sm: 8,
  md: 12,
  lg: 16,
  xl: 20,
  xxl: 24,
  full: 999,
};

// ─── Shadow Depth Layers ──────────────────────────────────────

// Extra small: for subtle chip / inline elements
export const shadowXs = {
  shadowColor: colors.shadowDark,
  shadowOffset: { width: 2, height: 2 },
  shadowOpacity: 0.18,
  shadowRadius: 3,
  elevation: 1.5,
};

// Small: default raised card
export const shadowSm = {
  shadowColor: colors.shadowDark,
  shadowOffset: { width: 4, height: 4 },
  shadowOpacity: 0.25,
  shadowRadius: 6,
  elevation: 3,
};

// Medium: elevated card or header
export const shadowMd = {
  shadowColor: colors.shadowDark2,
  shadowOffset: { width: 5, height: 5 },
  shadowOpacity: 0.32,
  shadowRadius: 8,
  elevation: 5,
};

// Large: FAB, modals
export const shadowLg = {
  shadowColor: colors.shadowDark2,
  shadowOffset: { width: 6, height: 6 },
  shadowOpacity: 0.36,
  shadowRadius: 10,
  elevation: 7,
};

// Extra large: prominent FAB
export const shadowXl = {
  shadowColor: colors.shadowDark2,
  shadowOffset: { width: 7, height: 7 },
  shadowOpacity: 0.38,
  shadowRadius: 12,
  elevation: 9,
};

// Inset shadow for inputs / pressed state
export const shadowInset = {
  shadowColor: colors.shadowInset,
  shadowOffset: { width: -2, height: -2 },
  shadowOpacity: 0.45,
  shadowRadius: 3,
  elevation: 1,
};

// ─── Dual-light edge helper ───────────────────────────────
// Reduced white highlight opacity from 0.65 → 0.36 to lower glare

export const lightEdge = (borderRadius: number = radius.xxl) => ({
  borderRadius,
  borderWidth: 0.5,
  borderColor: 'rgba(255,255,255,0.36)',
});

export const lightEdgeTop = (borderRadius: number = radius.xxl) => ({
  borderRadius,
  borderTopWidth: 1,
  borderLeftWidth: 1,
  borderColor: 'rgba(255,255,255,0.40)',
  borderRightWidth: 0,
  borderBottomWidth: 0,
});

// ─── Raised Neumorphism (凸起) ──────────────────────────
export const neuOut = {
  backgroundColor: colors.bg,
  borderRadius: radius.xxl,
  borderWidth: 0,
  ...shadowSm,
};

// ─── Inset Neumorphism (凹陷) ───────────────────────────
export const neuIn = {
  backgroundColor: colors.bg,
  borderRadius: radius.xxl,
  borderWidth: 0,
  ...shadowInset,
};

// ─── Warm Card (raised) ────────────────────────────────
export const neoCard = {
  backgroundColor: colors.bg,
  borderRadius: radius.xxl,
  borderWidth: 0,
  ...shadowSm,
};

// Elevated card (more prominence)
export const neoCardElevated = {
  backgroundColor: colors.bg,
  borderRadius: radius.xxl,
  borderWidth: 0,
  ...shadowMd,
};

// Strong raised element (primary action button)
export const neoButton = {
  backgroundColor: colors.accent,
  borderRadius: radius.full,
  borderWidth: 0.5,
  borderColor: 'rgba(255,255,255,0.24)',
  ...shadowMd,
};

// Inset / recessed (input field)
export const neoInput = {
  backgroundColor: colors.inputBg,
  borderRadius: radius.full,
  borderWidth: 0,
  ...shadowInset,
};

// Filter chip
export const neoFilterChip = (active: boolean, activeColor?: string) => ({
  paddingHorizontal: spacing.md,
  paddingVertical: spacing.sm - 2,
  borderRadius: radius.full,
  backgroundColor: active ? (activeColor || colors.accent) : colors.chipBg,
  borderWidth: 0,
  ...(active ? {} : shadowXs),
});

// Status badge (mini pill)
export const neoBadge = (bgColor: string) => ({
  paddingHorizontal: spacing.sm + 2,
  paddingVertical: spacing.xs,
  borderRadius: radius.full,
  backgroundColor: bgColor,
  overflow: 'hidden' as const,
});

// ─── Typography ───────────────────────────────────────────────

export const typography = {
  h1: { fontSize: 22, fontWeight: '700' as const, color: colors.textPrimary },
  h2: { fontSize: 18, fontWeight: '700' as const, color: colors.textPrimary },
  h3: { fontSize: 16, fontWeight: '600' as const, color: colors.textPrimary },
  body: { fontSize: 15, color: colors.textPrimary },
  bodySmall: { fontSize: 13, color: colors.textSecondary },
  caption: { fontSize: 12, color: colors.textMuted },
};
